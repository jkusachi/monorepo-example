{
  "name": "@demo/server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@demo/common": "1.0.0",
    "axios": "^0.26.0"
  },
  "scripts": {
    "dev": "tsc-watch --build --noClear --onSuccess \"node ./dist/index.js\"",
    "build": "tsc --build",
    "start": "yarn build && node dist/index.js"
  },
  "devDependencies": {
    "tsc-watch": "^4.6.0"
  }
}
